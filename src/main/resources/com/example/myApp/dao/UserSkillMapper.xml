<!--?xml version="1.0" encoding="UTF-8" ?-->
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd" >
<mapper namespace="com.example.myApp.dao.UserSkillMapper">
    <select id="selectAll" resultType="com.example.myApp.model.UserSkill"> <!-- 一覧表示 -->
        select user_id, skill_id, skillName, skillLevel, skillDetail, createdat, updatedat from myApp_UserSkills order by user_id
    </select>
</mapper>